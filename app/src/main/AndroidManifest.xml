<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.vanda.share">
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Resource_detailsActivity"
            android:theme="@style/AppTheme" />
        <activity android:name=".Goods_detailsActivity" />
        <activity android:name=".PublishActivity" />
        <activity android:name=".CertificateActivity" />
        <activity android:name=".UploadActivity" />
        <activity
            android:name=".RegisterActivity"
            android:label="@string/title_activity_main" />
        <activity
            android:name=".MainActivity"
            android:windowSoftInputMode="adjustPan">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity" />
        <activity android:name=".CollectionActivity" />
        <activity android:name=".MyPublishActivity" />
        <activity android:name=".MyUploadActivity" />
        <activity android:name=".MyDownloadActivity" />
        <activity android:name=".ContactUsActivity" />
        <activity android:name=".SettingActivity" />
        <activity android:name=".ExamActivity" />
        <activity android:name=".Postgraduate_examinationActivity"></activity>
    </application>

</manifest>